spring.application.name=PARCIAL
server.port=8091

# PostgreSQL - Neon
spring.datasource.url=jdbc:postgresql://ep-super-poetry-ai0qdhf4-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_bxcPGsA71tzl
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Thymeleaf
spring.thymeleaf.cache=false

# Multipart - carga de archivos (max 10 MB)
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=12MB

# NOTA: Si la tabla 'pago' tiene la columna 'archivo_bytes' de versiones anteriores,
# ejecuta en MySQL: DROP TABLE pago;  y reinicia el servidor para que Hibernate la recree.
